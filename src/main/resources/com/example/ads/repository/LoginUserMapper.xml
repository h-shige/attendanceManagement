<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.ads.repository.LoginUserMapper">
  <select id="selectByUserName" resultType="LoginUser">
    SELECT
      site_user.id,
      site_user.user_name,
      site_user.password,
      authority.name as authority_name
    FROM site_user INNER JOIN authority
    ON site_user.authority_id  = authority.id
    WHERE site_user.user_name = #{userName};
  </select>
</mapper>